# Copy this file to .env and fill in your own MySQL credentials.
# .env is gitignored; this template is committed.

DB_HOST=localhost
DB_PORT=3306
DB_USER=nmap_app
DB_PASSWORD=change_me
DB_NAME=nmap_scanner

# Seconds before an nmap scan is abandoned.
NMAP_TIMEOUT=120
